# Генерация ключей priv1.pem и publ1.pem

## Быстрая генерация

### Шаг 1: Сгенерируйте ключи на сервере

```bash
cd /var/www/finik
git pull origin main
npm run generate-keys
```

Или напрямую:
```bash
node generate-keys-simple.js
```

Это создаст два файла:
- `priv1.pem` - приватный ключ (НЕ ПЕРЕДАВАЙТЕ НИКОМУ!)
- `publ1.pem` - публичный ключ (отправьте в Финик)

**Важно:** Код автоматически использует `priv1.pem` из корня проекта.

### Шаг 2: Установите права доступа (Linux/Mac)

```bash
chmod 600 priv1.pem
chmod 644 publ1.pem
```

### Шаг 3: Добавьте в .gitignore

**ВАЖНО:** Никогда не коммитьте приватный ключ!

```bash
echo "priv1.pem" >> .gitignore
echo "publ1.pem" >> .gitignore
```

### Шаг 4: Отправьте публичный ключ в Финик

```bash
cat publ1.pem
```

Скопируйте весь вывод и отправьте в поддержку Финика с указанием:
- Окружение: PRODUCTION
- AccountId: (ваш AccountId)
- API Key: (первые 10 символов)

### Шаг 5: Обновите .env

Добавьте в `.env`:
```env
FINIK_API_KEY=<новый_api_key_от_финика>
FINIK_ACCOUNT_ID=<ваш_account_id>
FINIK_ENVIRONMENT=production
```

**Примечание:** 
- Код автоматически будет использовать `priv1.pem` из корня проекта
- НЕ нужно указывать `FINIK_PRIVATE_PEM` в `.env`, если используете файл `priv1.pem`
- Для совместимости также поддерживаются старые файлы `privat1` и `publ1` (без расширения)

### Шаг 6: Перезапустите сервер

```bash
pm2 restart finik-api
```

## Проверка

### Проверьте приватный ключ:

```bash
npm run check-key
```

Должно показать:
- ✅ Длина ключа: ~1700-1800 символов
- ✅ Начало ключа: ✅
- ✅ Конец ключа: ✅
- ✅ Ключ можно использовать для подписи

### Проверьте публичный ключ:

```bash
cat publ1.pem
```

Должен показать:
```
-----BEGIN PUBLIC KEY-----
MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...
(много строк)
...
-----END PUBLIC KEY-----
```

## Структура файлов

После генерации у вас будет:
```
/var/www/finik/
├── priv1.pem          ← Приватный ключ (используется кодом)
├── publ1.pem          ← Публичный ключ (отправьте в Финик)
├── .env               ← Настройки (API Key, AccountId)
└── ...
```

## Важно

- ✅ `priv1.pem` - храните в секрете, не передавайте никому
- ✅ `publ1.pem` - можно передавать, отправьте в Финик
- ✅ Код автоматически использует `priv1.pem` из корня проекта
- ✅ Не нужно указывать `FINIK_PRIVATE_PEM` в `.env`, если используете файл

